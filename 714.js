// Flattens an hierarchical map into a single level
function flattenMap(map) {
  
}

const input = {
  'a': {
    'b': {
      'c': 12,
      'd': 'Hello World'
    },
    'e': [1,2,3]
  }
};

console.log(flattenMap(input))